spring:
  datasource:
    url: jdbc:h2:mem:bomtest;MODE=MySQL;DATABASE_TO_LOWER=TRUE;CASE_INSENSITIVE_IDENTIFIERS=TRUE;DB_CLOSE_DELAY=-1
bom:
  auth:
    secret: test-secret-for-unit-tests
    admin-password: admin123
  open:
    api-key: bom-open-key
